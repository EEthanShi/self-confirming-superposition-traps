# E2 冻结配置（2026-08-27 凌晨，依据 dev A-D 报告冻结；此后门与网格不可改）

步长与时标（dev B/D 定）：eta_V=0.05, eta_a=0.05, sigma_actor 无, T=8000（core），
m 系列 {1,5,20,80}；神经臂 eta_V=0.02, eta_a=0.02, K=256, T=3000, burn_in=200,
floor_eps=0.05, ema=0.98。

## Final 网格
- core：δ 19 点 [0.05,0.95]；p0 41 点；basin 网格 m=20（warm/cold/dim3 三臂）；
  分离点二分 m∈{1,5,20,80}×19δ（κ 收敛曲线）。
- 干预：floor ε∈{0.05,0.1,0.15,0.2}@δ=0.3；entropy δ∈{0.2,0.5,0.8}（τ* 二分 +
  τ 网格相分类 + sink@τ=0.8）；ortho δ∈{0.3,0.6}×m∈{5,20,80}（λ* 收敛）+
  端点 gap@λ=1.5；replay p̄∈{0.2,0.5}@δ=0.3, m∈{20,80}。
- 神经（GPU）：dims{2,3}×δ∈{0.15,0.3,0.45,0.6}×p0 21 点×30 seeds，CRN 配对
  （同 (δ,p0,seed) 基across dims）。

## 门（G，冻结）
- G1 对照：dim3 全部 runs 无 trap（basin=1 率 100%，δ>0）。
- G2 域归属：m=80 warm 网格与 sign(p0-p_δ) 一致率 ≥99%（排除 |p0-p_δ|≤0.02 带）。
- G3 分离点：m=80 的 19 个 δ 点 |sep-p_δ|≤0.02。
- G4 熵：τ* ∈ [min((1-δ)/logφ,15/8), 5/2]（逐 δ）；sink@τ=0.8 误差 ≤0.01。
- G5 floor：|sep-(p_δ-ε)/(1-2ε)|≤0.02（逐 ε）。
- G6 ortho（iff）：m=80 时 |λ*-λ_crit|≤0.1；端点 gap 与 ±s_λ 误差 ≤0.01。
- G7 replay：m=80 时 |λ*-λ_pred|≤0.02。
- 神经臂预注册为 observed 层（报告边界曲线与配对对照，不设通过门）。
- κ 收敛系列、cold-restart 对照为 observed 层。
